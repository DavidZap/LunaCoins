import {describe,it,expect} from 'vitest'; import {calculateBudgetScenarioAvailable,calculateBudgetedExpenses,calculateRealAvailable,calculateRemainingBudget,calculateUnbudgetedExpenses,summary,budgetStatus} from './finance'; import type {Budget,Transaction} from './types';
const x=(type:any,amount:number)=>({id:'1',type,amount,date:'2026-09-01'} as any);
const transaction=(id:string,type:Transaction['type'],amount:number,categoryId:string):Transaction=>({...x(type,amount),id,categoryId} as Transaction);
const budgets:Budget[]=[{id:'budget-food',month:'2026-09',categoryId:'food',limit:1000},{id:'budget-furniture',month:'2026-09',categoryId:'furniture',limit:null}];
describe('cálculos financieros',()=>{it('calcula disponible y ahorro',()=>{const s=summary([x('income',1000),x('deduction',100),x('expense',300),x('savings',200),x('investment',100)],'2026-09');expect(s.available).toBe(300);expect(s.savingsRate).toBe(.3)});it('separa gastos con y sin presupuesto sin duplicarlos',()=>{const transactions=[transaction('1','income',5000,'income'),transaction('2','deduction',500,'deduction'),transaction('3','expense',1000,'food'),transaction('4','expense',700,'furniture'),transaction('5','savings',300,'save-1')];expect(calculateBudgetedExpenses(transactions,budgets,'2026-09')).toBe(1000);expect(calculateUnbudgetedExpenses(transactions,budgets,'2026-09')).toBe(700);expect(calculateBudgetScenarioAvailable(transactions,budgets,'2026-09')).toBe(3200);expect(calculateRealAvailable(transactions,budgets,'2026-09')).toBe(2500);expect(calculateRealAvailable(transactions,budgets,'2026-09')).toBe(calculateBudgetScenarioAvailable(transactions,budgets,'2026-09')-700)});it('calcula presupuesto restante solo con límites definidos',()=>{const transactions=[transaction('1','expense',250,'food'),transaction('2','expense',900,'furniture')];expect(calculateRemainingBudget(transactions,budgets,'2026-09')).toBe(750)});it('calcula presupuesto',()=>expect(budgetStatus(750,1000)).toEqual({percent:75,remaining:250}));});
