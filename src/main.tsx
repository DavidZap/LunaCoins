import {useEffect,useMemo,useState} from 'react'; import {createRoot} from 'react-dom/client'; import {BarChart3,WalletCards,Repeat2,Tags,History,Settings,Plus,Search,Download,Upload,Trash2,Sun,Moon,X} from 'lucide-react'; import {BarChart,Bar,PieChart,Pie,Cell,ResponsiveContainer,Tooltip,XAxis,YAxis,LineChart,Line,Legend} from 'recharts'; import {allCategories,demoBudgets,demoTransactions} from './data'; import {budgetStatus,money,summary} from './finance'; import type {Budget,Transaction,TransactionType} from './types'; import './styles.css';
const nav=[['Dashboard',BarChart3],['Movimientos',WalletCards],['Presupuesto',Tags],['Recurrentes',Repeat2],['Categorías',Tags],['Histórico',History],['Configuración',Settings]] as const;
const load=<T,>(key:string,fallback:T):T=>{try{return JSON.parse(localStorage.getItem(key)||'') as T}catch{return fallback}};
function App(){const [tab,setTab]=useState<typeof nav[number][0]>('Dashboard'),[month,setMonth]=useState('2026-09'),[transactions,setTransactions]=useState(()=>load('fp-tx',demoTransactions)),[budgets,setBudgets]=useState(()=>load('fp-budgets',demoBudgets)),[dark,setDark]=useState(()=>load('fp-dark',false)),[modal,setModal]=useState(false),[search,setSearch]=useState(''); useEffect(()=>{localStorage.setItem('fp-tx',JSON.stringify(transactions));localStorage.setItem('fp-budgets',JSON.stringify(budgets));localStorage.setItem('fp-dark',JSON.stringify(dark));document.documentElement.dataset.theme=dark?'dark':'light'},[transactions,budgets,dark]); const s=useMemo(()=>summary(transactions,month),[transactions,month]); const monthly=transactions.filter(t=>t.date.startsWith(month)); const expenseByCat=budgets.map(b=>{const c=allCategories.find(c=>c.id===b.categoryId)!;const spent=monthly.filter(t=>t.type==='expense'&&t.categoryId===b.categoryId).reduce((a,t)=>a+t.amount,0);return {...b,name:c.name,spent,...budgetStatus(spent,b.limit)}}); const add=(v:Omit<Transaction,'id'|'createdAt'|'updatedAt'>)=>{const now=new Date().toISOString();setTransactions(a=>[{...v,id:crypto.randomUUID(),createdAt:now,updatedAt:now},...a]);setModal(false)};
return <div className="app"><aside><div className="brand"><span>f</span> finanzas</div><nav>{nav.map(([name,Icon])=><button className={tab===name?'active':''} onClick={()=>setTab(name)} key={name}><Icon/> {name}</button>)}</nav><button className="theme" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>} {dark?'Modo claro':'Modo oscuro'}</button></aside><main><header><div><p className="eyebrow">TU PANORAMA FINANCIERO</p><h1>{tab}</h1></div><div className="header-actions"><input aria-label="Mes" type="month" value={month} onChange={e=>setMonth(e.target.value)}/><button className="icon" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button></div></header>{tab==='Dashboard'&&<Dashboard s={s} data={expenseByCat}/>} {tab==='Movimientos'&&<Movements transactions={transactions} month={month} search={search} setSearch={setSearch} onDelete={id=>setTransactions(x=>x.filter(t=>t.id!==id))}/>} {tab==='Presupuesto'&&<BudgetView data={expenseByCat} onChange={(id,limit)=>setBudgets(x=>x.map(b=>b.id===id?{...b,limit}:b))}/>} {tab==='Histórico'&&<HistoryView transactions={transactions}/>} {['Recurrentes','Categorías','Configuración'].includes(tab)&&<Secondary tab={tab} transactions={transactions} setTransactions={setTransactions} budgets={budgets} setBudgets={setBudgets}/>}</main><button className="new" onClick={()=>setModal(true)}><Plus/> <span>Nuevo movimiento</span></button>{modal&&<TransactionModal month={month} onClose={()=>setModal(false)} onSave={add}/>}<div className="mobile-nav">{nav.slice(0,5).map(([n,I])=><button className={tab===n?'on':''} onClick={()=>setTab(n)} key={n}><I/><small>{n}</small></button>)}</div></div>}
function Dashboard({s,data}:{s:ReturnType<typeof summary>,data:any[]}){const cards=[['Ingresos',s.income,'green'],['Deducciones',s.deductions,'orange'],['Gastos',s.expenses,'red'],['Ahorro',s.savings+s.investments,'purple'],['Disponible',s.available,'blue']];const pie=data.filter(x=>x.spent).map(x=>({name:x.name,value:x.spent}));return <><section className="cards">{cards.map(([n,v,c])=><article className={'card '+c} key={n as string}><p>{n}</p><strong>{money(v as number)}</strong></article>)}</section><section className="split"><article className="panel"><div className="panel-title"><div><p className="eyebrow">CONTROL DEL MES</p><h2>Presupuesto</h2></div><b>{money(data.reduce((a,x)=>a+x.limit,0)-s.expenses)} restante</b></div><div className="budget-list">{data.map(x=><div className="budget-row" key={x.id}><div className="line"><b>{x.name}</b><span>{money(x.spent)} / {money(x.limit)}</span></div><div className="progress"><i style={{width:`${Math.min(x.percent,100)}%`}} className={x.percent>=100?'danger':x.percent>=75?'warn':''}/></div><small className={x.remaining<0?'bad':''}>{x.remaining<0?`Excedido en ${money(-x.remaining)}`:`Disponible ${money(x.remaining)}`} · {x.percent.toFixed(0)}%</small></div>)}</div></article><article className="panel chart"><h2>Gastos por grupo</h2><ResponsiveContainer width="100%" height={280}><PieChart><Pie data={pie} dataKey="value" nameKey="name" innerRadius={62} outerRadius={95}>{pie.map((_,i)=><Cell fill={['#705cf6','#39b68a','#f6af4f','#ef6b73','#50a6dd'][i%5]} key={i}/>)}</Pie><Tooltip formatter={(v)=>money(Number(v||0))}/><Legend/></PieChart></ResponsiveContainer></article></section><section className="split"><article className="panel chart"><h2>Presupuesto vs. gasto real</h2><ResponsiveContainer width="100%" height={260}><BarChart data={data}><XAxis dataKey="name" hide/><YAxis tickFormatter={v=>`${v/1000}k`}/><Tooltip formatter={(v)=>money(Number(v||0))}/><Bar dataKey="limit" fill="#dedbf9" radius={5}/><Bar dataKey="spent" fill="#705cf6" radius={5}/></BarChart></ResponsiveContainer></article><article className="panel insights"><p className="eyebrow">INSIGHTS</p><h2>Lo importante este mes</h2>{data.filter(x=>x.percent>=75).slice(0,3).map(x=><p key={x.id}>• Has utilizado el {x.percent.toFixed(0)}% del presupuesto de <b>{x.name}</b>.</p>)}<p>• Tu tasa de ahorro es <b>{(s.savingsRate*100).toFixed(1)}%</b>.</p></article></section></>}
function Movements({transactions,month,search,setSearch,onDelete}:{transactions:Transaction[],month:string,search:string,setSearch:(x:string)=>void,onDelete:(id:string)=>void}){const rows=transactions.filter(t=>t.date.startsWith(month)&&`${t.description}${t.categoryId}`.toLowerCase().includes(search.toLowerCase()));return <section className="panel"><div className="panel-title"><div><p className="eyebrow">REGISTRO DEL MES</p><h2>Movimientos</h2></div><label className="search"><Search/><input placeholder="Buscar" value={search} onChange={e=>setSearch(e.target.value)}/></label></div><div className="table">{rows.map(t=>{const c=allCategories.find(c=>c.id===t.categoryId);return <div className="tx" key={t.id}><div className={'dot '+t.type}/><div><b>{t.description}</b><small>{c?.name||'Sin categoría'} · {new Date(t.date+'T12:00').toLocaleDateString('es-CO')}</small></div><strong className={t.type==='income'?'income':''}>{t.type==='income'?'+':'−'} {money(t.amount)}</strong><button className="delete" onClick={()=>onDelete(t.id)} title="Eliminar"><Trash2/></button></div>})}</div></section>}
function BudgetView({data,onChange}:{data:any[],onChange:(id:string,limit:number)=>void}){return <section className="panel"><div className="panel-title"><div><p className="eyebrow">LÍMITES MENSUALES</p><h2>Presupuesto</h2></div><button className="ghost">Copiar mes anterior</button></div><div className="budget-table"><div className="thead"><span>Categoría</span><span>Tope mensual</span><span>Gastado</span><span>Disponible</span><span>Estado</span></div>{data.map(x=><div className="tr" key={x.id}><b>{x.name}</b><input type="number" min="0" value={x.limit} onChange={e=>onChange(x.id,Number(e.target.value))}/><span>{money(x.spent)}</span><span className={x.remaining<0?'bad':''}>{money(x.remaining)}</span><span className={x.percent>=100?'pill red':x.percent>=75?'pill yellow':'pill green'}>{x.percent.toFixed(0)}%</span></div>)}</div></section>}
function HistoryView({transactions}:{transactions:Transaction[]}){const months=['2026-07','2026-08','2026-09'];const data=months.map(month=>summary(transactions,month));return <section className="panel"><p className="eyebrow">COMPARA TU PROGRESO</p><h2>Histórico financiero</h2><ResponsiveContainer width="100%" height={300}><LineChart data={data}><XAxis dataKey="month"/><YAxis tickFormatter={x=>`${x/1e6}M`}/><Tooltip formatter={(v)=>money(Number(v||0))}/><Legend/><Line dataKey="income" name="Ingresos" stroke="#705cf6" strokeWidth={3}/><Line dataKey="expenses" name="Gastos" stroke="#ef6b73" strokeWidth={3}/><Line dataKey="savings" name="Ahorro" stroke="#39b68a" strokeWidth={3}/></LineChart></ResponsiveContainer><div className="budget-table"><div className="thead"><span>Mes</span><span>Ingresos</span><span>Gastos</span><span>Ahorro</span><span>Disponible</span></div>{data.map(x=><div className="tr" key={x.month}><b>{new Date(x.month+'-02T12:00').toLocaleDateString('es-CO',{month:'long',year:'numeric'})}</b><span>{money(x.income)}</span><span>{money(x.expenses)}</span><span>{money(x.savings+x.investments)}</span><span>{money(x.available)}</span></div>)}</div></section>}
function Secondary({tab,transactions,setTransactions,budgets,setBudgets}:{tab:string,transactions:Transaction[],setTransactions:(x:Transaction[])=>void,budgets:Budget[],setBudgets:(x:Budget[])=>void}){const exportJSON=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify({transactions,budgets},null,2)],{type:'application/json'}));a.download='respaldo-finanzas.json';a.click()};return <section className="panel"><p className="eyebrow">{tab.toUpperCase()}</p><h2>{tab==='Configuración'?'Tus datos, siempre contigo':tab==='Recurrentes'?'Movimientos programados':'Categorías disponibles'}</h2>{tab==='Configuración'?<div className="settings"><button onClick={exportJSON}><Download/> Crear respaldo JSON</button><label><Upload/> Importar respaldo<input type="file" accept="application/json" onChange={e=>{const f=e.target.files?.[0];if(f)f.text().then(x=>{const d=JSON.parse(x);setTransactions(d.transactions||[]);setBudgets(d.budgets||[])})}}/></label><button className="danger-btn" onClick={()=>{setTransactions(transactions.filter(t=>!t.demo));setBudgets([])}}><Trash2/> Eliminar datos de demostración</button></div>:tab==='Categorías'?<div className="category-grid">{allCategories.map(c=><div key={c.id}><b>{c.name}</b><span>{c.group}</span></div>)}</div>:<p className="empty">Configura ingresos y gastos periódicos. Próximamente se copiarán al iniciar cada mes.</p>}</section>}
function TransactionModal({month,onClose,onSave}:{month:string,onClose:()=>void,onSave:(x:any)=>void}){const [type,setType]=useState<TransactionType>('expense'),[amount,setAmount]=useState(''),[categoryId,setCategoryId]=useState('food'),[description,setDescription]=useState(''),[error,setError]=useState('');const choices=allCategories.filter(c=>type==='deduction'?c.type==='deduction':type==='savings'||type==='investment'?c.type==='savings':c.type==='expense');useEffect(()=>setCategoryId(choices[0]?.id||''),[type]);const save=()=>{if(!amount||Number(amount)<=0||!description){setError('Ingresa una descripción y un valor mayor a cero.');return}onSave({date:`${month}-01`,type,categoryId,description,amount:Number(amount),account:'Cuenta principal',paymentMethod:'Débito',notes:''})};return <div className="overlay"><form className="modal" onSubmit={e=>{e.preventDefault();save()}}><button type="button" className="close" onClick={onClose}><X/></button><p className="eyebrow">REGISTRO RÁPIDO</p><h2>Nuevo movimiento</h2><label>Tipo<select value={type} onChange={e=>setType(e.target.value as TransactionType)}>{[['expense','Gasto'],['income','Ingreso'],['deduction','Deducción'],['savings','Ahorro'],['investment','Inversión'],['debt','Pago de deuda']].map(([v,n])=><option value={v} key={v}>{n}</option>)}</select></label><label>Valor<input autoFocus inputMode="numeric" type="number" min="1" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="Ej. 45000"/></label><label>Categoría<select value={categoryId} onChange={e=>setCategoryId(e.target.value)}>{choices.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label>Descripción<input value={description} onChange={e=>setDescription(e.target.value)} placeholder="Ej. Restaurante"/></label>{error&&<small className="bad">{error}</small>}<button className="save">Guardar movimiento</button></form></div>}
createRoot(document.getElementById('root')!).render(<App/>);
